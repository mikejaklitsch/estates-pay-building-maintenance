# EPBM — charging + display snapshot. Both are 7-way fanouts over the
# estate-fanout macros in epbm_effects.txt.

# ═══════════════════════════════════════════════
# Transfer the saved monthly amount to each estate. Scope: country.
# Reads var:epbm_pay_* (set by calculate_maintenance). Runs monthly.
# ═══════════════════════════════════════════════

epbm_charge_estates = {
	epbm_charge_estate_pay = { estate = nobles }
	epbm_charge_estate_pay = { estate = clergy }
	epbm_charge_estate_pay = { estate = burghers }
	epbm_charge_estate_pay = { estate = peasants }
	epbm_charge_estate_pay = { estate = tribes }
	epbm_charge_estate_pay = { estate = dhimmi }
	epbm_charge_estate_pay = { estate = cossacks }
}

# ═══════════════════════════════════════════════
# Snapshot pay vars into show vars so tooltips keep displaying last
# month's charged amount after the next recalc overwrites pay vars.
# Scope: country. Player-only.
# ═══════════════════════════════════════════════

epbm_snapshot_for_display = {
	epbm_snapshot_estate = { estate = nobles }
	epbm_snapshot_estate = { estate = clergy }
	epbm_snapshot_estate = { estate = burghers }
	epbm_snapshot_estate = { estate = peasants }
	epbm_snapshot_estate = { estate = tribes }
	epbm_snapshot_estate = { estate = dhimmi }
	epbm_snapshot_estate = { estate = cossacks }
}
